---
description: Show communication mode status (use "/communication reset" to return to default)
---
If the user's arguments ($ARGUMENTS) contain the word "reset", call the `communication_reset` tool for the current session and report the result briefly. Otherwise call the `communication_status` tool and report the result briefly in plain language.
